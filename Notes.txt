PYTHON
-----------------------------------
Python-1
1989-govido van Rossum
-open source programing language
-high level programing language
dynamic,oops

--------------------------------
python-2
Letss start!!
--------------------------------
python-3

MODULES AND PIP

module-it's like a python library module where you can use  code written by somebody.
two types-
1.built in modules-no need install it seprately..it's already pre install in the python language(Hashlib)
2.Externl modules - modules are imported from the third party file..it can be installed by using pip command(pandas,tensorflow,numpy)

pip-to install py module (pip install pandas )
-------------------------------
python-4

print()
print - it's an function used to print the output.
() - it invokes the print function.
--------------------------------------------------------------------------
python -5

Comments, Escape Sequences & Print Statement

Comments- python interpreter igoners the commented line to ecxcute. 
				for signal line- #
				for mulitple lines - '''   '''

 Escape Sequences - to insert the character that can be directly used in a string, back slash \ followed by character.
					\n - to print next line
					\" \" - gives qotes insed the print statement

More on print statement- sep="-" ,it sperates each word with -
						end = " ",it prints at the end and by default it used as a next line
--------------------------------------------------------------------------
python -6

Variables and Data Types

Variables- it is an container used to hold the data.
Data Types -type of value the variable holdes
			1.numeric data - int,float,complex
			2.text data - str(string)
			3.boolean data - true or false
			4.Sequnced data - list,tuple
						list-ordered collection of data,sperated by  (comma,) ,enclosed within square brackets[]. (Mutable-changable)
						tuple -ordered collection of data,sperated by  (comma,),enclosed within parentheses () . (immutable-Cannot be modified)
			5.Mapped data - dict
							unordered collection of data containg key value pairs encloed within currly brackets{}.
--------------------------------------------------------------------------
python -7

Arithmetic operators
			addition : +
			subtraction : -
			multiplication : *
			exponational : **
			division : /       (gives qutoitent)
			floor division : //
			modulus : % (gives remainder)

Assignment : Create a basic calculator

--------------------------------------------------------------------------
python-8

solution for assignment (check in the python-8.py file for the solution).
--------------------------------------------------------------------------
python-9

Typecasting-converting one type of data to another type by using functions like int(),float(),str(),hex(),oct(),tuple(),set(),list(),dict() etc.
types:
		1. explicit typecasting : the convertion is done by the programer by using the typecasting function.
		2.Implicit typecasting : the convertion is done automatically by python (priority wise).
		
--------------------------------------------------------------------------
python-10

Taking User Input : we can user input by using the input() function.
			syntax :   variable :  input() 
					by default it takes the input as string. for this we can typecasting while taking user input.
--------------------------------------------------------------------------
python-11

Strings-anything inside the single or double quotes are considered as string. (like in the form of array of characters)
		for multiline stirng use - """  """(use triple double quotesor single)
		
		Looping through string can be done.
--------------------------------------------------------------------------
python-12


Strings Slicing and Operations on Strings

Length of a string can be found using - len() function

Slicing in String- name[1:5] 1 is included but 5 is not included.
--------------------------------------------------------------------------
python-13

String Methods-set of built in methods to alter or modify strings.
Strings are immutable.
--------------------------------------------------------------------------
python-14

Conditional operators: > , < , >= ,<= ,== ,!=

If Else Conditional Statements  (Identation matters!!)
 
if
if-else
if-else-elif
nested if-else-elif
--------------------------------------------------------------------------
python-15

Time module(built in module)- to print the local time (System time)
by  using function time.strftime('%H,%M,%S') ...hour,min,sec
--------------------------------------------------------------------------
python-16

Match Case Statements (similar to if-else)

Comparing variable to all the patterns
switch case statement - used in one or more cases
--------------------------------------------------------------------------
python-17

Loops
programer wants to exceute certain items for n number of times by using loops.

for loop - iterate over a sequence of iterable objects ..such as strings,lists,tuples,sets,dictionaries. 
			range() - range for a specific number  (function)
--------------------------------------------------------------------------
python-18

While Loops	- it excutes while the condition  is true..if the condition fails it comes out from the loop. We need to increment or decrement.
Else with while loop - we can use the else statement with the while loop.
there is no do-while loop in python.
--------------------------------------------------------------------------
python-19

break and continue

break statement- Breaks the loop and leaves the loop  (terminates the loop within).  loop ko chodkar nikal jav.
continue statement - skips the iteration and continues.
--------------------------------------------------------------------------
python-20

Functions- it's a block of code that performs specific task whenver its  called.

built-it functions :  min() ,max(),len(),sum() etc..
user defined functions : the functions created by the user by using def
							def functionname(parameter):
									pass           #it wont show error for the empty func
									
---------------------------------------------------------------------------
python-21

Function Arguments - default arguments - we can add default values inside the def function..if there is no values given it prints default values
					 keyword arguments - we can provide arguments in key=value.
					 variable length arguments - in def function wwe can create  tuple or dictionaries like 
					                             ..def avg(*num) -> tuple ,def name(**names) -> dictionaries
					 required arguments - if we don't mention value in the def() thn we shld provide the value (whn key = value syntax not used).

return statement - it returns the value in the function by calling function.

---------------------------------------------------------------------------
python-22

Introduction to Lists-  ordered collection of data items...to store mulitple items in a single variable.[1,3,4,'vikas',5]
						we can alter them after creation.
						-unique index starts from 0
---------------------------------------------------------------------------
python-23


List Methods:  list.sort()  #sort in ascending order
			   list.append() #add element in the end of the list
			   list.sort(reverse=True) #sort in decending order
			   list.reverse() #reverse the list
			   list.index() #index of an element
			   list.count() #number of occurance of an element.
			   list.insert(index,value ) #to insert  an elements  in a particulur index
			   list.copy() #copy of the list
			   list.extend() #it extends the list and add the elements of other variable in it
			   list.remove() #remove of element.
---------------------------------------------------------------------------
python-24

Tuples-cannot be changed. immutable
	   enclosed within ()
	   ordered collection
---------------------------------------------------------------------------
python-25

Operations on Tuples- if you want to add,remove,change the tuple thn you must convert the tuple to a list and perfrom the operations in the list 
					   later the list back to the tuple. --> temp = list(tup)
					- Concatitaion can be done in tuple bcz it shhld create in new tuple
					- count()  
					- index()
					-len()
---------------------------------------------------------------------------
python-27

create a program capable of displaying questions to the user like kbc(kon banege kordpathi)
-use list data types to store question and answers.
-display final amount after the comlepting the game.
---------------------------------------------------------------------------
python-28

f-strings(formating String) - String formating mechanism..where we can pass the value of an variable inside the string.{name}
---------------------------------------------------------------------------
python-29

Docstrings and pep 8

Docstrings- String that appers right after def we see it as comments like triple quotes ''' hi''' -> its called as docstirng. 
			printed by using __doc__
			-comments that are completely igonred by the python interpreter but the comments used right after declare the function are considered 
			  as Docstrings. it only considers (''' ''').
			  
PEP 8 - (python enhancement proposal )its a document describes new features.

import this - its zen of python by Tim Peters it prints poem which as meagning full. (Easter egg)

---------------------------------------------------------------------------
python-30

Recursion - process of defining something  in terms of itsel.
			wkt,function can call other..and it also possible for the function to call itsel-recursive function.

---------------------------------------------------------------------------
python-31

Sets - unordered collection of data, separated by comma and enclosed within currly brackets.
	   -don't contain duplicate items..only contains unique items in the set
---------------------------------------------------------------------------
python-32

Set Methods : union() #combination of set 
			  update() #moves elements from set1 to set2
			  
			  intersection() #common elements in both sets
			  intersection_update() #it only keeps the common elements.
			  
			  symmetric_difference() #prints items that are not similar bascially  prints uniques items
			  symmetric_difference_update(s) #it removes the elments common 
			  
			  difference() #prints items that are only present in original set not in both the sets
			  difference_update() #it updates the cities3 set
			  
			  isdisjoint() #checks if items of the set is present in another returns in boolean
			  issuperset() #checks if all the elements  of a set are present is original set
			  issubset() #checks subset
			  
			  add() #adds item to the set
			  update() #adds all items of a  set
			  
			  remove() #removes item ..if the item not present in the set it rises an error
			  discard()  #removes item ..if the item not present in the set it doesn't rises an error
			  pop() #removes a random item in a set bcz sets are unordered
			  del cities5 #deletes the entire set
			  clear() #clears the entries set..basically makes it an empty set 
--------------------------------------------------------------------------------------------------------------------------
python-33

dictionaries - ordered collection of data
			   stores multiple items in a variable
			   key:value pairs  (not vice verse : you cannot acces the key by using the value)
			   separated with comma and enclsoed within currly  brackets {}
			   
			   print(dict.keys()) #prints all the key
			   print(dict.values()) #prints all the values
			   print(dict.items()) #prints keys with vlaues
--------------------------------------------------------------------------------------------------------------------------
python-34

dictionary methods : update() #to add  all the key : value pairs
					 clear() #clears the dict
					 pop() #remove 
					 popitem() #removes last key:value pair
					 del emp #deletes the entire dict
					 del emp['a'] #delets only a keyvlaue
					 
					 refer python documentation to know more about everything
--------------------------------------------------------------------------------------------------------------------------
python-35


for Loop with else - else clause is used along with if statement but python also to use else with for and while loops as well.
					 -the program exits the group only after the else block is exceuted.
					 - if we use a break statement in the loop thn else statement will not be exceute.
--------------------------------------------------------------------------------------------------------------------------
python-36

Exception Handling - process of responding to unwanted or unexpected events whn a computer program runs.
				   - deals with the events to avoid the programming or system crashing.
 
					- try-except  : ValueError,IndexError
--------------------------------------------------------------------------------------------------------------------------
python-37

Finally Clause - it's also a part of exception handling.
			   - whn we handle try and except we can include finally block at the end.. the finally block is excuted.
			   - used like a closing resource or a database connection or a ending program.
			   - it always excetues..the right way to use it in the def function. (even if there is return also ..it exceutes)
--------------------------------------------------------------------------------------------------------------------------
python-38

Raising custom errors -  we can raise custom error by using the raise keyword.
--------------------------------------------------------------------------------------------------------------------------
python-40
			   
			   Exercise : codeing and decoding
			   
--------------------------------------------------------------------------------------------------------------------------
python-41

short hand if else - there is a short hand syntax for the if else statement.
					- you can multiple else statement in single line
					- readablity
					- if you want assign a value to a variable based on a condition.
					- not suitable for complex situations
--------------------------------------------------------------------------------------------------------------------------
python -42

Enumerate Function - it's a built-in function that allows you to loop over a sequence(list,tuple,string) and get index and value 
					 of each element at a same time.
				   - it's start  the index at 0...but we can specify the starting index 
				   -  we can use it whn we want  to perform some action with both index and value of each element
--------------------------------------------------------------------------------------------------------------------------
python-43

Virtual Environment -  it is a tool used to isolate specific python environments on a single machine allowing to work on multiple 
					   projects and different dependicies.
					 - basically you can create different virtual environment on your system for different versions of the modules or dependices 
					 to work
					 -  python -m env myenv #create a virutal environment
					 -   sourcemyenv/bin/activate # to activate the env (on mac)
--------------------------------------------------------------------------------------------------------------------------
python-44

How import works - process of loading code from a python module into current script
				 - to import , we use import statement followed by  the name of the module
				 -once the module is imported we can use any functions and variables defined in the module
			     - we can also import our files function into the current script by using import filename
--------------------------------------------------------------------------------------------------------------------------
python-45

if __name__ == "__main__"    -idiom is a common pattern used in python scripts  to determine whether the script is being run directly    or         		 		  being imported as as module 
			                 - usefull tool for oraganizing and seperating code that shld be run directly from the code the shld be imported
							 and used as a module.
							 - it allows you to reuse code from a script by importing it as a module into another script 
							 ,without running the code in the original script.
--------------------------------------------------------------------------------------------------------------------------
python-46

os module - built-in library provides functions for interacting with the operating system. (import os)
			- reading , writing files, interacting with file system & running commands.
--------------------------------------------------------------------------------------------------------------------------
python-48

Local vs Global Variables 

local variable - its is a variable that is defined within a function and is only accessible within that function.
				- it is created whn the function is called ..and it will destory whn the function returns
				
Global Variables - it is defined outside the function and it is accessible within any function.

global keyword - used to modify global variable within a function.
--------------------------------------------------------------------------------------------------------------------------
python-49

File IO in Python - python provides several ways to mainpulate files.
				  - we must open the file first using open function with two arguments : name of file and mode 
							mode : r - read : opens the file for reading only..shows error if the file not exsit.(default mode)
								   w -  write :  opens the file for writing only and creates a new file if the file doesn't exsit.
								                 -write mode clears the previous data in the file and write  freshly.
								   a - appending : opens the file for appending only and creates a new file if the file doesn't exsit.
								   x - create  : creates a file and gives error if the file already exists. 
								   t - text : used to handle txt files (default)
								   b - binary : used to handle binary files(images,pdfs..etc )
								'with' statement : by using this 'with'  there is no need to close the data.
--------------------------------------------------------------------------------------------------------------------------
python-50

read(), readlines() and other methods

readline() - this method reads a single line from the file.if we want to read multiple lines we can use a loop  
writeline() - writes a sequence of strings to a file , it doesn't add newlines inbetween 
--------------------------------------------------------------------------------------------------------------------------
python-51

seek(), tell() and other functions : these functions are used to work with the file objects and their positions witin the function.

seek() : allows you to move the current position(bytes) within a file to a specific point,you can move either forward or backward.

tell() : it returns the current position in the  file.

truncate() : truncate the file to specific size by using truncate function.(manage the size of the file).
--------------------------------------------------------------------------------------------------------------------------
python -52

Lambda functions : anonyomous function without a name,defined using lambda keyword
				   lambda arguments : expression 
--------------------------------------------------------------------------------------------------------------------------
python-53

Map, Filter and Reduce: higher - order functions...it use functions as a arguments 
					   map() : applies a function to each element in asequence and returns a new sequeance
					          - maps values to a function
							   syntax : map(function_name , iterable)
					  filter() : it filters a sequence of elements bassed on a given function(predictable)
									 syntax : filter(predictable , iterable)
					 reduce() : to use this we need to import it first (from functools import reduce)
								-takes in two arguments and returns singel value.
--------------------------------------------------------------------------------------------------------------------------
python-54

'is' vs '=='  :   both are comparison operates that can be used to check if two  values are equal.
					'is' - compares exact location of object in memory
					'==' - compares the value.
--------------------------------------------------------------------------------------------------------------------------
python-55

snake-water-gun game
--------------------------------------------------------------------------------------------------------------------------
python-56

Introduction to OOPs  :  mainly there are two approaches write a code or program
							1) procedural programming - wat we are following till now
							2) object-oriented programming - is to use classes and objects to represent real-world concepts and entites
							
OOPs:
	class : it is a blueprint or a template for creating objects.
	object : instance of a class,it contains it won data and methods
	
	key features:
				encapsulation : internal state of an object is hidden and can only be accessed or modified through the object's method.
				inheritance	: 	which allows new classes to be created that inherit the properites and method of an exsisting class.	
				
--------------------------------------------------------------------------------------------------------------------------
python-57

Classes and Objects
	class : it is a blueprint or a template for creating objects.
			- user defined objects are created using the class keyword
	object : instance of a class used to access the properites of the class
	
self parameter : it is reference to the current instance of the class,and is used to access variables that belong to the class.
				- it must be provided as the extra parameter inside the method defination

--------------------------------------------------------------------------------------------------------------------------
python-58

Constructors : special method in class used to create and initialize an object of a class.
			 - it is invoked automatically when an object is created.
			 -purpose is to initialize or assign values to the data memebers of the class...cannot return any values.
			    syntax  :  def __init__(self):
		
		types : 1) parameterized Constructors : when the Constructors accepts arguments along with self
												-this arguments used to assign the values to the data members
				2) Default Constructors : when the Constructors doesn't accept any arguments from the object
										it as only one arguments self in the Constructors
		
--------------------------------------------------------------------------------------------------------------------------
python-59

Decorators : to modify the behavior of functions and methods.
			 it is a function that takes another func as an argument and returns a new function that modifies the behavior original func.

--------------------------------------------------------------------------------------------------------------------------

